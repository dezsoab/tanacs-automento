---
interface Props {
  eyebrow: string;
}
const { eyebrow } = Astro.props;
---

<div class="mb-14">
  <p class="text-orange-500 text-xs font-bold tracking-widest uppercase mb-3">{eyebrow}</p>
  <h2 class="font-display font-black text-4xl md:text-5xl text-white">
    <slot />
  </h2>
</div>
